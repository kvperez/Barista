order Car {
    hopper (roast name, pump year) {
        shot.name = name
        shot.year = year
    }

    item getYear() -> pump {
        serve shot.year
    }
}

Car myCar = new Car("Ford", 2014)
remake(myCar.getYear() + "years old")